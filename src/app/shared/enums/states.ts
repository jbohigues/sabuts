export enum GameStatus {
  pending = 0,
  in_progress = 1,
  finished = 2,
}

export enum FriendRequestStatus {
  pending = 0,
  accepted = 1,
  rejected = 2, // lo vamos a necesitar?
}

export enum RoundStatus {
  in_progress = 0,
  completed = 1,
}
